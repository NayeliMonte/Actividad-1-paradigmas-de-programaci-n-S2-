#include <stdio.h>

int main() {
    int h;
    int d;
 
    printf("Programa para calcular cuántos km nadará un atún según las horas\n"); 
    printf("Ingrese las horas nadando: ");
    scanf("%d", &h); 
    
    d = h * 24;
 
    printf("La distancia recorrida por el atún es: %d km\n", d); 
 
    return 0;
}